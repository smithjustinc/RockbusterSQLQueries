The following queries utilize subqueries to identify aggregated values and the number of top customers in each country.

Queries:
SELECT B.city, A.country, E.customer_id, D.first_name, D.last_name, SUM(E.amount) AS total_amount_paid FROM country A INNER JOIN city B ON A.country_id = B.country_id INNER JOIN address C ON B.city_id = C.city_id INNER JOIN customer D ON C.address_id = D.address_id INNER JOIN payment E ON D.customer_id = E.customer_id WHERE B.city IN ('Aurora', 'Atlixco', 'Xintai', 'Adoni', 'Dhule (Dhulia)', 'Kurashiki', 'Pingxiang', 'Sivas', 'Sivas', 'Celaya', 'So Leopoldo') GROUP BY E.customer_id, D.first_name, D.last_name, city, country ORDER BY SUM(amount) DESC LIMIT 5

SELECT AVG(total_amount_paid) AS average FROM (SELECT B.city, A.country, E.customer_id, D.first_name, D.last_name, SUM(E.amount) AS total_amount_paid FROM country A INNER JOIN city B ON A.country_id = B.country_id INNER JOIN address C ON B.city_id = C.city_id INNER JOIN customer D ON C.address_id = D.address_id INNER JOIN payment E ON D.customer_id = E.customer_id WHERE B.city IN ('Aurora', 'Atlixco', 'Xintai', 'Adoni', 'Dhule (Dhulia)', 'Kurashiki', 'Pingxiang', 'Sivas', 'Sivas', 'Celaya', 'So Leopoldo') GROUP BY E.customer_id, D.first_name, D.last_name, city, country ORDER BY SUM(amount) DESC LIMIT 5) AS total_amount_paid

SELECT customers.country, customers.all_customer_count, top_customers.top_customer_count FROM (SELECT DISTINCT E.country, COUNT(DISTINCT A.customer_id) AS all_customer_count FROM customer A INNER JOIN payment B ON A.customer_id = B.customer_id INNER JOIN address C ON A.address_id = C.address_id INNER JOIN city D ON C.city_id = D.city_id INNER JOIN country E ON D.country_id = E.country_id GROUP BY country ORDER BY all_customer_count DESC) AS customers INNER JOIN (SELECT top.country, COUNT(top.customer_id) AS top_customer_count FROM (SELECT A.customer_id, A.first_name, A.last_name, D.city, E.country, SUM(B.amount) AS total_amount_paid FROM customer A INNER JOIN payment B ON A.customer_id = B.customer_id INNER JOIN address C ON A.address_id = C.address_id INNER JOIN city D ON C.city_id = D.city_id INNER JOIN country E ON D.country_id = E.country_id WHERE city IN ('Aurora', 'Atlixco', 'Xintai', 'Adoni', 'Dhule (Dhulia)', 'Kurashiki', 'Pingxiang', 'Sivas', 'Sivas', 'Celaya', 'So Leopoldo') GROUP BY A.customer_id, A.first_name, A.last_name, D.city, E.country ORDER BY total_amount_paid DESC LIMIT 5) AS top GROUP BY top.country) AS top_customers ON customers.country = top_customers.country ORDER BY top_customer_count DESC
